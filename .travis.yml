language: ruby
rvm:
  - 2.2.4

script: bundle exec lyp compile --install --env test/assert_test.ly

cache:
  bundler: true
  directories:
    - $HOME/.lyp
    
env:
  - LILYPOND_VERSION=2.18.2
#  - LILYPOND_VERSION=2.19.35
