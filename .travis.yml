language: ruby
rvm:
  - 2.2.4

install:
  - gem install lyp

script: lyp compile test/assert_test.ly # lyp -v # test --install --env

cache:
  bundler: true
  directories:
    - $HOME/.lyp
    
env:
  - LILYPOND_VERSION=2.18.2
  - LILYPOND_VERSION=2.19.35
